name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t kithung/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true kithung/react-test npm test

      - run: docker build -t kithung/multi-client ./client
      - run: docker build -t kithung/multi-nginx ./nginx
      - run: docker build -t kithung/multi-server ./server
      - run: docker build -t kithung/multi-worker ./worker

      - run: docker push kithung/multi-client
      - run: docker push kithung/multi-nginx
      - run: docker push kithung/multi-server
      - run: docker push kithung/multi-worker
